<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Circle Image Animation</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/assets/application.css"> <!-- Link to the external CSS file -->
  <link rel="stylesheet" href="/assets/pages_navigate.css"> <!-- Link to the new external CSS file -->
</head>
<body>
  <h1><%= current_user.username %>'s <span style="color: gray;">Things</span></h1>

  <p>
    Number of Topics: <%= Topic.where(user_id: current_user.id).count %> (<%=Topic.minimum(:created_at).strftime("%Y/%m/%d")%> - <%=Topic.maximum(:created_at).strftime("%Y/%m/%d")%>) <i class="fa-solid fa-grip-vertical"></i>
    Number of Notes: <%= Note.where(user_id: current_user.id).count %> (<%=Note.minimum(:created_at).strftime("%Y/%m/%d")%> - <%=Note.maximum(:created_at).strftime("%Y/%m/%d")%>)
  </p>

  <!-- Year selection form -->
  <div class="center-container">
    <%= form_with(url: root_path, method: :get, local: true) do %>
      <div class="mb-3 d-flex align-items-center form-group">
        <%= button_tag params[:show_chart] == "true" ? "Hide Activities Chart" : "View Activities Chart", class: 'btn btn-primary', id: 'chart-button' %>

        <%= label_tag :year, 'Select Year:', class: 'form-label me-2' %>
        <%= select_tag :year, options_for_select(Topic.pluck(Arel.sql('DISTINCT EXTRACT(YEAR FROM created_at)')).map(&:to_i).sort.reverse, params[:year] || Topic.maximum(Arel.sql('EXTRACT(YEAR FROM created_at)')).to_i), class: 'form-select', style: 'width: auto; padding-right: 1.5rem;', onchange: 'this.form.submit()' %>

        <%= hidden_field_tag :show_chart, params[:show_chart] == "true" ? "false" : "true" %>
      </div>
    <% end %>
  </div>

  <div class="table-container position-relative">
    <!-- Table displaying data for the selected year -->
    <table class="table table-bordered mb-0">
      <thead>
        <tr>
          <% (1..12).each do |month| %>
            <th><%= Date::MONTHNAMES[month] %></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <tr>
          <% selected_year = params[:year] ? params[:year].to_i : Date.today.year %>
          <% (1..12).each do |month| %>
            <% topics_count = Topic.where(user_id: current_user.id, created_at: Date.new(selected_year, month).all_month).count %>
            <% notes_count = Note.where(user_id: current_user.id, created_at: Date.new(selected_year, month).all_month).count %>
            <td>
              <% if topics_count > 0 || notes_count > 0 %>
                <span class="highlighted"><%= topics_count %> | <%= notes_count %></span>
              <% end %>
            </td>
          <% end %>
        </tr>
      </tbody>
    </table>
  </div>

  <!-- Display the generated chart image -->
  <% if @chart_image_path.present? && params[:show_chart] == "true" %>
    <div id="chart-container" class="d-flex justify-content-center" style="width: 100%; height: 400px; margin-top: 20px;">
      <img src="/<%= @chart_image_path %>" alt="Topics Chart for <%= @selected_year %>" class="img-fluid">
    </div>
  <% end %>

  <div class="content-wrapper">
    <div class="circle-container">
      <div class="circle-item first">
        <div class="circle-image">
          <a href="/topics">
            <img src="#" alt="Troves">
          </a>
        </div>
        <div class="circle-caption">Troves</div>
      </div>
      <div class="circle-item middle">
        <div class="circle-image">
          <a href="/tools">
            <img src="#" alt="Tools">
          </a>
        </div>
        <div class="circle-caption">Tools</div>
      </div>
      <div class="circle-item last">
        <div class="circle-image">
          <a href="teases">
            <img src="/teases" alt="Teases">
          </a>
        </div>
        <div class="circle-caption">Teases</div>
      </div>
    </div>
  </div>
</body>
</html>
