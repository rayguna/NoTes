<%= form_with(model: tool) do |form| %>
  <% if tool.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tool.errors.count, "error") %> prohibited this tool from being saved:</h2>

      <ul>
        <% tool.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :search_query, style: "display: block" %>
    <%= form.text_field :search_query %>
  </div>

  <div>
    <%= form.label :results, style: "display: block" %>
    <%= form.text_field :results %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
