<!DOCTYPE html>
<html>
<head>
  <title>YourAppName</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag 'paint_brush', media: 'all' %> <!-- Link to your custom CSS file -->
</head>
<body>
  <p class="text-success"><%= notice %></p>

  <div class="container mt-4">
    <div class="container mt-4">
      <div class="row mb-4">
        <!-- Heading -->
        <div class="col-md-8">
          <h1>Topics</h1>
        </div>

        <!-- Search Form in Dropdown -->
        <div class="col-md-4">
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="searchDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Search
            </button>
            <div class="dropdown-menu p-3" aria-labelledby="searchDropdown">
              <%= search_form_for @q, url: notes_path, method: :get, html: { class: "form-inline" } do |f| %>
                <div class="form-group">
                  <%= f.label :topic_name_cont, "Topic contains", class: "sr-only" %>
                  <%= f.search_field :topic_name_cont, class: "form-control mr-2", placeholder: "Topic" %>
                </div>
                <div class="form-group">
                  <%= f.label :title_cont, "Title contains", class: "sr-only" %>
                  <%= f.search_field :title_cont, class: "form-control mr-2", placeholder: "Title" %>
                </div>
                <div class="form-group">
                  <%= f.label :content_cont, "Content contains", class: "sr-only" %>
                  <%= f.search_field :content_cont, class: "form-control mr-2", placeholder: "Content" %>
                </div>
                <%= f.submit "Search", class: "btn btn-primary" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- pagination -->
    <nav aria-label="Page navigation example" class="mt-4">
      <ul class="pagination justify-content-center">
        <%= paginate @topics, theme: 'bootstrap-4' %>
      </ul>
    </nav>

    <div class="row">
      <% @topics.each do |topic| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title"><span class="highlight"><%= topic.name %></span></h5>
              <p class="card-text"><small class="text-muted">Topic: <%= topic.name %></small></p>
              <p class="card-text"><small class="text-muted">Created At: <%= topic.created_at.strftime("%B %d, %Y") %></small></p>
              <a href="<%= topic_path(topic) %>" class="position-absolute" style="top: 10px; right: 10px;"><i class="fa-solid fa-eye"></i></a>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>
