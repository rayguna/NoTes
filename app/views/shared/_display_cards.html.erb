<div class="row">
  <% table_name.each do |row| %>
    <div class="col-md-4 mb-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title"><span class="highlight"><%= row.name %></span></h5>
          <p class="card-text"><small class="text-muted">Topic: <%= row.name %></small></p>
          <p class="card-text"><small class="text-muted">Created At: <%= row.created_at.strftime("%B %d, %Y") %></small></p>
          <a href="<%= url_for([row, { topic_type: params[:topic_type], display_as: params[:display_as] }]) %>" class="position-absolute" style="top: 10px; right: 10px;">
            <i class="fa-solid fa-eye"></i>
          </a>
        </div>
      </div>
    </div>
  <% end %>
</div>
